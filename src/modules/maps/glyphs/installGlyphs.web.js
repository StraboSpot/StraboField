// Web uses Mapbox-hosted glyphs (see GLYPHS_URL) and has no filesystem, so there is
// nothing to install. Stub keeps the bundled glyph assets out of the web bundle.
const installGlyphs = async () => {};

export default installGlyphs;
