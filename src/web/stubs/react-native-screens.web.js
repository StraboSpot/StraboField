// Web stub for react-native-screens
// react-navigation's Screens.js checks `if (Screens != null)` before using screen components,
// so returning null here causes it to fall back to plain <View> wrappers.
module.exports = null;
