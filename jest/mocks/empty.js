// Stub for side-effect-only native/polyfill modules (e.g. react-native-get-random-values) that
// install globals at import time. Tests here don't rely on those globals, so a no-op is enough.
module.exports = {};
